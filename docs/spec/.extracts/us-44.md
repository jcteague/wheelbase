---
plan: us-44
source: plans/us-44/
extracted_at: 2026-06-20
status: complete
---

# Extract: us-44

## Summary

US-44 adds the first persisted IVR storage path to the app: a SQLite `ivr_snapshot` table, a main-process collector service (`collectIVRSnapshots`) that batches active-position tickers through the existing Barchart scraper, and a shared scheduler registration that runs the collection job after market close. It also exposes a manual `Refresh IVR now` action in Settings so the renderer can trigger the same batch path on demand and receive a typed summary of the run. (plans/us-44/plan.md)

## Architecture Decisions

### ADR: Migration numbering 007, not 008
- **Decision:** Create the snapshot migration as `migrations/007_create_ivr_snapshot.sql`, not `008_create_ivr_snapshot.sql`.
- **Why:** The repo currently contains `006_add_credential_settings.sql` and `008_create_pending_assignments.sql`, with no `007` file present. The migration runner sorts filenames lexicographically and applies any unseen file, so filling the gap keeps the sequence contiguous and avoids baking the stale story note into the implementation.
- **Alternatives considered:** Keep `008` as written in the story. Rejected because `008` is already taken in the worktree, and renumbering a later migration would create unnecessary churn.
- **Source:** plans/us-44/research.md

### ADR: Barchart as the canonical IVR source
- **Decision:** Build the collector on top of `fetchIVR` from `src/main/integrations/barchart-ivr-scraper.ts`, and persist `source = 'barchart'`.
- **Why:** The codebase already ships a Barchart scraper with a typed `IVRResult` union and `IVRDataSchema` whose `source` literal is `'barchart'`. The US-44 story has already been corrected to match that implementation direction.
- **Alternatives considered:** Add an abstraction layer for multiple IVR vendors. Rejected because the story only requires one source, and no other IVR provider exists in the current app.
- **Source:** plans/us-44/research.md

### ADR: Same-day overwrite semantics
- **Decision:** Implement same-day overwrite as a transaction that deletes any existing `ivr_snapshot` rows for `(underlying, UTC-calendar-date(observed_at))` before inserting the fresh row.
- **Why:** The table primary key is `(underlying, observed_at)`, so a second run on the same day with a later timestamp will not naturally replace the earlier row. Explicit delete-then-insert matches the story requirement that the latest same-day value wins and keeps the SQL simple under SQLite.
- **Alternatives considered:** Change the primary key to `(underlying, observed_date)` or use a synthetic upsert key. Rejected because the story already fixes the table shape and downstream read paths benefit from retaining the exact observation timestamp.
- **Source:** plans/us-44/research.md

### ADR: Collector-level throttling boundary
- **Decision:** Enforce the 1 request/second politeness rule in `src/main/services/ivr-collector.ts`, even though `fetchIVR` already throttles internally.
- **Why:** The story explicitly requires the collector to own the rate limit so concurrent callers cannot bypass it. A collector-local queue or sequential loop with a sleep boundary makes batch behavior deterministic and guarantees spacing across the whole collection job.
- **Alternatives considered:** Rely only on the scraper module's module-level limiter. Rejected because separate collector invocations could still interleave in ways the story forbids.
- **Source:** plans/us-44/research.md

### ADR: Active-underlying selection
- **Decision:** Derive collection targets from the `positions` table by selecting distinct `ticker` values where `status != 'CLOSED'`, then normalize to uppercase and de-duplicate before fetches.
- **Why:** The story defines the batch as "all active-position underlyings", and the existing schema already tracks `ticker`, `phase`, and `status` on `positions`. This keeps the collector independent from renderer list queries and aligned with SQLite as source of truth.
- **Alternatives considered:** Reuse `listPositions()` or inspect `phase` only. Rejected because `listPositions()` computes renderer-facing fields the collector does not need, and `status` is the clearest current marker for excluding closed wheels.
- **Source:** plans/us-44/research.md

### ADR: Manual trigger integration via dedicated IVR IPC surface
- **Decision:** Add a dedicated IVR IPC surface: main handler in `src/main/ipc/ivr.ts`, preload exposure under `window.api.ivr.collectNow()`, renderer adapter in `src/renderer/src/api/ivr.ts`, and a small mutation hook consumed by `SettingsPage`.
- **Why:** Existing feature areas use dedicated IPC namespaces (`assignments:*`, `settings:*`, `market-data:*`) rather than overloading unrelated handlers. A separate IVR namespace keeps `settings.ts` thin while still allowing the Settings page to host the button.
- **Alternatives considered:** Add `'ivr:collect-now'` directly inside `src/main/ipc/settings.ts` or call the scheduler from the renderer without a typed adapter. Rejected because both options blur feature boundaries and work against the existing typed preload/api pattern.
- **Source:** plans/us-44/research.md

### ADR: Non-trading-day guard inside the collector
- **Decision:** Put the non-trading-day guard at the top of `collectIVRSnapshots`, returning an `ok` batch summary with zero successes and a skip indicator when `BrokerProvider.getMarketStatus()` reports `session: 'closed'` and the date is not a trading day.
- **Why:** The story requires the guard to cover both the scheduled path and manual trigger. Keeping it in the collector means the scheduler registration and the manual IPC both share one code path and one logging decision.
- **Alternatives considered:** Rely on `afterClose` scheduling alone or put the guard only in the IPC handler. Rejected because the manual trigger must also be safe on weekends and holidays.
- **Source:** plans/us-44/research.md

### ADR: Settings-page placement
- **Decision:** Add the "Refresh IVR now" action to the existing `Market Data` section of `src/renderer/src/pages/SettingsPage.tsx` as a secondary action with inline success/error feedback.
- **Why:** No US-44 mockup exists, so the safest path is to extend the existing market-data management surface rather than invent a new page or panel. The page already contains secondary actions, lightweight messages, and vendor-adjacent operational controls.
- **Alternatives considered:** Create a new settings subsection or put the trigger in the positions page. Rejected because the story explicitly names Settings and does not justify a larger navigation or layout expansion.
- **Source:** plans/us-44/research.md

## Contracts

### ivr:collect-now
- **Type:** IPC handler
- **Shape:** request/response (exact from contracts/ivr-collect-now.md)

Request — payload: none
```ts
ipcRenderer.invoke('ivr:collect-now')
```

Response envelope:
```ts
type IpcCollectIVRNowResult =
  | {
      ok: true
      batch: {
        successCount: number
        errorCount: number
        skippedCount: number
        skippedReason: 'market_closed' | null
      }
    }
  | {
      ok: false
      errors: Array<{ field: string; code: string; message: string }>
      code?: string
    }
```

Main-process responsibilities: (1) call `scheduler.runNow('ivr-collect')`; (2) ensure the registered `ivr-collect` job handler returns the batch summary from `collectIVRSnapshots(...)`; (3) return that batch summary to the renderer unchanged. The handler must not accept renderer-supplied tickers or timing overrides — target selection remains in the main-process collector. Unlike `assignments:run-detection-now`, this channel must return the collector summary because US-44 requires the renderer to see success and error counts.
- **Source:** plans/us-44/contracts/ivr-collect-now.md
- **Implementation:** src/main/ipc/ivr.ts

### CollectIvrNowBatchSchema
- **Type:** Zod schema
- **Shape:**
```ts
export const CollectIvrNowBatchSchema = z.object({
  successCount: z.number().int().min(0),
  errorCount: z.number().int().min(0),
  skippedCount: z.number().int().min(0),
  skippedReason: z.enum(['market_closed']).nullable()
})
```
No request schema is required because the channel takes no payload.
- **Source:** plans/us-44/contracts/ivr-collect-now.md
- **Implementation:** src/main/schemas.ts

### CollectIvrNowResult (renderer adapter shape)
- **Type:** other (renderer API type)
- **Shape:**
```ts
export type CollectIvrNowResult = {
  successCount: number
  errorCount: number
  skippedCount: number
  skippedReason: 'market_closed' | null
}
```
The renderer API helper in `src/renderer/src/api/ivr.ts` normalizes the envelope to this shape and throws the existing `ApiError` shape on `{ ok: false }`, matching the project's current renderer API pattern.
- **Source:** plans/us-44/contracts/ivr-collect-now.md
- **Implementation:** src/renderer/src/api/ivr.ts

### ivr-collect (scheduler job)
- **Type:** event (scheduler job registration)
- **Shape:** Registered before `scheduler.start()`:
```ts
scheduler.register({
  name: 'ivr-collect',
  cadence: { kind: 'afterClose', offsetMinutes: 60 },
  handler: async () => ... // resolve current broker with brokerFactory.create(), then collectIVRSnapshots({ db, brokerProvider, logger })
})
```
- **Source:** plans/us-44/plan.md
- **Implementation:** src/main/index.ts

### CollectIVRSnapshotsResult (service result)
- **Type:** other (service result type)
- **Shape:**

| Field | Type | Notes |
| --- | --- | --- |
| `successCount` | `number` | Rows successfully inserted after overwrite handling. |
| `errorCount` | `number` | Tickers that produced parse/network/rate-limit/invalid-input failures. |
| `skippedCount` | `number` | Tickers intentionally not persisted, including `not_available` outcomes. |
| `skippedReason` | `'market_closed' \| null` | `'market_closed'` when the entire batch exits early on a non-trading day; otherwise `null`. |

Counting rules: `ok` persisted → `successCount`; `not_available` → `skippedCount` + INFO log + no row; `parse_error`/`network_error`/`rate_limited`/`invalid_input` → `errorCount`, continue batch; weekend/holiday early exit before any fetch → `successCount=0, errorCount=0, skippedCount=0, skippedReason='market_closed'`.
- **Source:** plans/us-44/data-model.md
- **Implementation:** src/main/services/ivr-collector.ts

## Schema Changes

### ivr_snapshot
- **Change:** new table + new migration + new secondary index
- **Columns / fields:**

| Field | Type | Required | Notes |
| --- | --- | --- | --- |
| `underlying` | `TEXT` | Yes | Uppercase ticker symbol from `positions.ticker`. |
| `observed_at` | `TEXT` | Yes | ISO-8601 timestamp returned by `fetchIVR(...).data.observedAt`. |
| `ivr` | `TEXT` | Yes | Decimal string with 1 decimal place, from the numeric scraper result. |
| `ivp` | `TEXT` | No | Decimal string with 1 decimal place when Barchart returns percentile. |
| `iv30` | `TEXT` | No | Decimal string for 30-day historical volatility when provided. |
| `source` | `TEXT` | Yes | Constant `'barchart'` (`source TEXT NOT NULL DEFAULT 'barchart'`). |

  - Primary key: `(underlying, observed_at)`
  - Secondary index: `(underlying, observed_at DESC)` for latest-snapshot lookups in US-45
  - Validation: `underlying` non-empty and uppercase before persistence; `ivr` constrained to `0..100` via `IVRDataSchema`; `ivp`/`iv30` nullable; `source` persisted exactly as `'barchart'`.
  - Same-day overwrite rule: before inserting a fresh row, delete any existing row for the same `underlying` whose `observed_at` falls on the same UTC calendar date as the new row.
- **Source:** plans/us-44/data-model.md
- **Migration file:** migrations/007_create_ivr_snapshot.sql

## Acceptance Criteria

(Verbatim scenario names / Gherkin from docs/epics/06-stories/US-44-ivr-snapshot-store-and-scheduler.md)

- Background: a migration creates table `ivr_snapshot` with columns `underlying TEXT NOT NULL`, `observed_at TEXT NOT NULL` (ISO-8601), `ivr TEXT NOT NULL` (decimal string, 1 dp), `ivp TEXT` (nullable), `iv30 TEXT` (nullable), `source TEXT NOT NULL DEFAULT 'barchart'`, `PRIMARY KEY (underlying, observed_at)`; and an index exists on `(underlying, observed_at DESC)`.
- **Collector runs once per market day after close** — Given the scheduler is configured to run at 17:00 ET on trading days, When the scheduler fires, Then `collectIVRSnapshots()` is invoked.
- **Collector picks up all active-position underlyings** — Given positions exist for tickers `["SPY", "AAPL", "TSLA"]` in phases other than CLOSED, When `collectIVRSnapshots` runs, Then it calls `fetchIVR` for each distinct underlying And requests are spaced at least 1 second apart.
- **Successful snapshot is persisted** — Given `fetchIVR` returns `{ status: "ok", data: { ticker: "SPY", ivr: 42.5, ivp: 50.0, iv30: 0.18, observedAt: "2026-05-29T21:05:00Z" } }`, When the collector persists the result, Then a row is inserted into `ivr_snapshot` for `("SPY", "2026-05-29T21:05:00Z")` with ivr `"42.5"`.
- **Re-running on the same calendar day overwrites the existing row** — Given an `ivr_snapshot` already exists for SPY on 2026-05-29, When the collector runs again the same day, Then the existing row's `observed_at` and `ivr` are replaced with the fresh values And no exception propagates from the unique constraint.
- **Not-available ticker is recorded but with no row written** — Given `fetchIVR` returns `{ status: "not_available" }`, When the collector handles the result, Then it does not insert a row for that ticker And an INFO log records "ticker not covered by Barchart IVR" with the symbol.
- **Parse error is logged and the collector continues to the next ticker** — Given `fetchIVR` returns `{ status: "parse_error" }`, When the collector handles the result, Then a WARN log is emitted And the collector continues to the next ticker without aborting the batch.
- **Manual trigger from settings** — Given a renderer-only IPC handler `"ivr:collect-now"`, When the user clicks "Refresh IVR now" in settings, Then the collector runs immediately for all active-position underlyings And the result of the batch (success count, error count) is returned to the renderer.
- **Market is closed on a non-trading day** — Given today is a recognised market holiday or weekend, When `collectIVRSnapshots` runs (whether via scheduler or manual trigger), Then it calls `BrokerProvider.getMarketStatus()` and detects a non-trading day And the collector exits without making any network requests And an INFO log records the skip reason.

## Decisions & Tradeoffs

- Collector service signature accepts `{ db, brokerProvider, logger, fetchIvr?, clock? }` — `fetchIvr` and `clock` are optional injectable seams for tests (fake timers / clock seam). (plans/us-44/plan.md, plans/us-44/tasks.md)
- Active targets sorted alphabetically before iteration to preserve deterministic batch order. (plans/us-44/data-model.md)
- Scheduler job uses `afterClose` cadence with `offsetMinutes: 60`. The `afterClose` cadence also skips non-trading days at the scheduler level, so the collector's trading-day guard is belt-and-suspenders for the scheduled path but essential for the manual path. (plans/us-44/plan.md, docs/epics/06-stories/US-44-ivr-snapshot-store-and-scheduler.md)
- `ivr:collect-now` manual trigger calls `scheduler.runNow('ivr-collect')`, which resets the cadence clock to now after the out-of-band run. (docs/epics/06-stories/US-44-ivr-snapshot-store-and-scheduler.md)
- Job handler resolves the active broker via `brokerFactory.create()` at handler-run time rather than capturing a stale provider. (plans/us-44/plan.md)
- Settings page UX: success summary shows `successCount`/`errorCount`; `skippedReason === 'market_closed'` shows a muted/warning-tone message rather than a success message; rejected mutation uses the existing `getApiErrorMessage(error)` pattern. Renderer logic kept thin (no ticker selection, batch assembly, or market-status branching in the component). (plans/us-44/plan.md)
- Refactor: extracted a small local `MessageText` helper in `SettingsPage.tsx` so IVR and Massive status messages render through one path with shared tone-class logic, reducing drift. code-simplifier agent was not used. (plans/us-44/refactor-phase-results.md)
- E2E suite boots the real Electron app with a fake-IVR seam enabled via `WHEELBASE_FAKE_IVR` and reuses the existing test-only scheduler IPC (`_test:scheduler-*` / `_test:scheduler-registry`), staying offline rather than hitting live Barchart. Each `it()` maps to exactly one AC. (plans/us-44/tasks.md, plans/us-44/quickstart.md, e2e/ivr-collector.spec.ts)
- Epic 12 had originally planned an `iv_snapshot` table holding raw 30-day IV values with IVR computed locally; this story stores IVR snapshots (the computed value) directly because Barchart exposes IVR in the scraper response. The Epic 12 plan will be revised in a separate update. (docs/epics/06-stories/US-44-ivr-snapshot-store-and-scheduler.md)

## Source Code References

(Verified to exist via Read.)

- migrations/007_create_ivr_snapshot.sql
- src/main/services/ivr-collector.ts
- src/main/services/ivr-collector.test.ts
- src/main/db/migrate.test.ts
- src/main/index.ts
- src/main/index.test.ts
- src/main/services/polling-scheduler.ts
- src/main/services/polling-scheduler.test.ts
- src/main/services/scheduler-instance.ts
- src/main/integrations/barchart-ivr-scraper.ts
- src/main/ipc/ivr.ts
- src/main/ipc/ivr.test.ts
- src/main/ipc/test-scheduler.ts
- src/main/schemas.ts
- src/preload/index.ts
- src/preload/index.d.ts
- src/renderer/src/api/ivr.ts
- src/renderer/src/api/ivr.test.ts
- src/renderer/src/hooks/useCollectIvrNow.ts
- src/renderer/src/pages/SettingsPage.tsx
- src/renderer/src/pages/SettingsPage.test.tsx
- e2e/ivr-collector.spec.ts

## Open Questions

- The trading-day guard only detects weekends, not weekday market holidays — deferred to a follow-up; see docs/epics/06-stories/followup-ivr-trading-day-calendar.md.
- Out of scope for US-44 (per story): IVR computation from raw IV time series, watchlist underlyings (only active-position underlyings collected), historical backfill (first day of use has only what the daily run produces), and real-time IVR streaming. (docs/epics/06-stories/US-44-ivr-snapshot-store-and-scheduler.md)
