---
plan: us-31
source: plans/us-31/
extracted_at: 2026-05-31
status: complete
---

> Manually reconciled 2026-06-27 to the Alpaca→Massive migration; this extract now diverges from its original plan dir (re-running plan-extractor would revert it).

# Extract: us-31

<!-- generated:from us-31 -->

## Summary

Builds a provider-agnostic `MarketDataProvider` interface with a `MassiveMarketDataProvider` implementation (plus a `FakeMarketDataProvider` for tests/dev) supporting REST request/response (stock quotes, option snapshot, option chain snapshot) and WebSocket streaming (aggregate-minute stock bars via JSON). The `marketDataFactory` object abstracts provider construction so downstream services never import the concrete class. This is the foundational integration layer for all Epic 06 live market data stories. Streaming uses RxJS Observables so downstream consumers get first-class unsubscription, error/completion channels, and operators like `retry`, `share`, and `debounceTime`. (Source: `plans/us-31/plan.md`)

> Reconciliation note (2026-06-27): The market-data layer migrated Alpaca→**Massive** (a Polygon-compatible API). The concrete implementer is now `MassiveMarketDataProvider` (`src/main/integrations/massive-market-data.ts`) over the global `fetch` and a single `wss://delayed.massive.com/stocks` WebSocket — there is no `AlpacaMarketDataProvider`. Account info, broker activities, and market status moved off this interface onto a separate `BrokerProvider` (`broker:*` IPC); the only `Alpaca*` class left is `AlpacaBrokerProvider`, which implements that separate interface.

## Architecture Decisions

### ADR: Alpaca SDK — use for REST, bypass for streaming

- **Decision:** Use `@alpacahq/typescript-sdk` (v0.0.32-preview) for REST calls where it works correctly; bypass it with raw `ws` WebSocket clients for streaming. Keep working endpoints on the SDK rather than rewriting them.
- **Why:** The SDK is a Deno-to-Node transpile (via `dnt`), marked as no longer maintained. Several methods have bugs or incomplete types (`getStocksSnapshots` hits wrong path; `getOptionsSnapshots` type omits `greeks`/`impliedVolatility`; `getActivity` ignores query params; WebSocket streaming is "todo"). The core REST client (`getAccount`, `getClock`, `getStocksQuotesLatest`, `getActivity`) works. Building a full replacement HTTP client would be wasted effort.
- **Alternatives considered:** Replace SDK entirely with raw `fetch` (too much work for endpoints that already work); `alpaca-trade-api-js` (older, callback-based, worse TypeScript support).
- **Source:** `plans/us-31/research.md`

> Reconciliation note (2026-06-27): Superseded by the Massive migration. The market-data layer no longer uses the Alpaca SDK at all — `MassiveMarketDataProvider` talks to the Polygon-compatible `https://api.massive.com` REST API over the global `fetch` (appending `apiKey` to each request) with no SDK dependency. The Alpaca SDK now survives only behind the separate `AlpacaBrokerProvider`.

### ADR: Streaming via raw `ws` package with two dedicated sockets

- **Decision:** Use the `ws` npm package with two independent WebSocket connections — one for stock data at `wss://stream.data.alpaca.markets/v2/{dataFeed}` (JSON frames), one for option data at `wss://stream.data.alpaca.markets/v1beta1/{optionFeed}` (MessagePack binary frames). Multiplex all symbol subscriptions over each connection.
- **Why:** Alpaca SDK has zero WebSocket support. Node's built-in `WebSocket` (Node 21+) may differ in Electron's bundled Node version. `ws` is battle-tested, works identically in Electron main process and Vitest, and is trivially mockable. Alpaca allows only 1 concurrent connection per endpoint, so multiplexing is required.
- **Alternatives considered:** Node built-in WebSocket (Electron's bundled Node may not match; less mockable); `socket.io` (wrong protocol, Alpaca uses raw WebSocket).
- **Source:** `plans/us-31/research.md`

> Reconciliation note (2026-06-27): The two-socket design is superseded. The current provider opens a **single** `ws` WebSocket to `wss://delayed.massive.com/stocks` carrying JSON text frames for aggregate-minute (`AM`) stock bars. There is no option WebSocket. The `ws` package is still used for the reasons above (trivially mockable in Vitest).

### ADR: RxJS Observables for the streaming interface

- **Decision:** Use RxJS `Observable` for `stream(feed, symbols)` — REST methods remain plain `Promise`-returning functions. Errors flow through the Observable error channel as `StreamError`; unsubscribe via `subscription.unsubscribe()` which sends the WebSocket unsubscribe message via Observable teardown.
- **Why:** Hand-rolled callback + manual subscription registry is a less capable Observable. RxJS provides (1) first-class unsubscription via `Subscription`, (2) built-in error/completion channels — no separate `onStreamError` callback, (3) operators downstream stories need: `retry`/`retryWhen` for US-38 reconnection, `share`/`shareReplay` for multicasting in US-32–34, `distinctUntilChanged` and `debounceTime` for throttling. Native `Observable` (WICG, Chromium 135+) only exists in the renderer — the main process is Node.js, which has no native Observable.
- **Alternatives considered:** Native Observable (not available in Node.js/Electron main process, missing critical operators); callbacks with manual registry (reimplements Observable badly; downstream stories would hand-roll retry/multicast); AsyncIterables (pull-based, wrong model for push-based WebSocket streams).
- **Dependency impact:** RxJS is ~30KB tree-shakeable; only `Observable`, `Subject`, and a few utility functions needed here.
- **Source:** `plans/us-31/research.md`

### ADR: MessagePack decoding via `@msgpack/msgpack`

- **Decision:** Use `@msgpack/msgpack` for decoding option stream binary frames. Use `decodeMulti()` (not `decode()`) because Alpaca batches messages as arrays per frame.
- **Why:** Standard MessagePack implementation for JavaScript, works with Node `Buffer` directly (extends `Uint8Array`), and is the library Alpaca documentation references. `decode()` throws `RangeError` if a buffer contains multiple packed objects — `decodeMulti()` handles Alpaca's batching.
- **Alternatives considered:** `msgpack-lite` — older, less maintained, same API surface.
- **Source:** `plans/us-31/research.md`

> Reconciliation note (2026-06-27): No longer applies. The Massive stream is JSON text frames only (Polygon-compatible shapes); there is no binary/MessagePack decoding path. `@msgpack/msgpack` remains in `package.json` as a leftover from the original Alpaca version but is unused by the current provider.

### ADR: Greeks/IV available via REST snapshot only; OI not available at all

- **Decision:** `OptionSnapshot.greeks` and `OptionSnapshot.iv` come from REST `/v1beta1/options/snapshots` only — stream events never carry them. `openInterest` and `volume` are typed `number | null` and Alpaca always returns `null` for both (not available in any Alpaca option endpoint).
- **Why:** Alpaca's option streaming only provides quote (bid/ask) and trade (price/size) data. Greeks/IV come from the REST snapshot. Open interest is absent from all Alpaca option endpoints.
- **Alternatives considered:** Derive volume from `getOptionsBars()` (possible but adds complexity); OI would need a different data source entirely.
- **Source:** `plans/us-31/research.md`

> Reconciliation note (2026-06-27): Under Massive, `OptionSnapshot.greeks` and `OptionSnapshot.impliedVolatility` are **optional** (`greeks?` / `impliedVolatility?`) and populated only when Massive returns them on the REST options snapshot; `openInterest` and `volume` remain typed `number | null` and are `null` from the snapshot endpoint. There is no option stream at all.

### ADR: Derive market session client-side from clock + calendar

- **Decision:** Derive session type (`regular`, `pre`, `post`, `closed`) by comparing the clock timestamp against calendar open/close times plus known extended-hours windows. Pre-market is 4:00–9:30 AM ET, post-market is 4:00–8:00 PM ET.
- **Why:** Alpaca's `/v2/clock` only returns `is_open` (boolean), `next_open`, and `next_close`. There is no `session` field. The AC requires session as one of `regular | pre | post | closed`.
- **Alternatives considered:** Only returning `is_open` boolean — insufficient for the AC.
- **Source:** `plans/us-31/research.md`

> Reconciliation note (2026-06-27): Market status (and account info / broker activities) is no longer part of the `MarketDataProvider` interface. It moved onto a separate `BrokerProvider` served by the `broker:market-status` IPC channel (there is no `market-data:market-status` channel).

### ADR: Structured `MarketDataError` class with discriminating `code`

- **Decision:** Define `MarketDataError` extending `Error` with a typed `code` field. Codes: `auth_failed`, `network_error`, `rate_limited`, `stream_disconnected`, `streaming_unsupported`, `subscription_failed`, `unknown`. Throw (don't return) errors, consistent with the rest of the codebase.
- **Why:** Consistent with the story's AC requiring structured errors with specific codes. Services can pattern-match on `error.code` without parsing message strings.
- **Alternatives considered:** Returning `Result<T, E>` tuples — cleaner functionally but inconsistent with the rest of the codebase which uses thrown errors.
- **Source:** `plans/us-31/research.md`, `plans/us-31/data-model.md`

> Reconciliation note (2026-06-27): The code set is now mapped from HTTP status against Massive's REST API: `auth_failed`, `network_error`, `not_found`, `rate_limited`, `streaming_unsupported`, `unknown` (`401/403 → auth_failed`, `404 → not_found`, `429 → rate_limited` after `MAX_RETRIES` honouring `Retry-After`). The original `stream_disconnected` and `subscription_failed` codes are gone; `not_found` was added.

### ADR: Keep existing `alpaca.ts` but mark `@deprecated`

- **Decision:** Keep `src/main/integrations/alpaca.ts` as-is during this story. The new `AlpacaMarketDataProvider` creates its own SDK client internally. Add `@deprecated` JSDoc to `client` and `resetClient` exports. Removal happens when downstream stories migrate callers.
- **Why:** Deleting immediately risks breaking the build if any downstream branch depends on it. Deprecation signals intent without disruption.
- **Alternatives considered:** Delete immediately — safe today (no active imports) but unnecessarily risky.
- **Source:** `plans/us-31/research.md`

> Reconciliation note (2026-06-27): Still kept and `@deprecated`, but it no longer backs the market-data provider (which is now `MassiveMarketDataProvider`). Account info / activities / market status now flow through the separate `BrokerProvider` (`broker:*` IPC) and `AlpacaBrokerProvider`.

### ADR: Provider constructor owns SDK client lifecycle

- **Decision:** `AlpacaMarketDataProvider` constructor accepts `{ keyId, secretKey, paper, dataFeed?, optionFeed? }` and creates the Alpaca SDK client internally via `createClient({ key, secret, paper })`. `environment: 'paper' | 'live'` is derived from the constructor's `paper` flag, not from any API response.
- **Why:** Alpaca's `getAccount()` has no paper/live indicator. The constructor is the only place this fact is known reliably.
- **Source:** `plans/us-31/research.md`, `plans/us-31/data-model.md`

> Reconciliation note (2026-06-27): Superseded by the Massive migration. `MassiveMarketDataProvider` takes a `MassiveMarketDataConfig = { apiKey: string }` and appends `apiKey` to each REST request — there is no SDK client and no paper/live flag on the market-data provider. The `environment` / paper-vs-live concern now lives on the separate `BrokerProvider` / `AlpacaBrokerProvider`.

## Contracts

### `MarketDataProvider` interface

- **Type:** TypeScript interface (integration contract)
- **Shape:**

  ```typescript
  interface MarketDataProvider {
    getStockQuotes(tickers: string[]): Promise<Map<string, StockQuote>>
    getOptionSnapshot(contractId: string): Promise<OptionSnapshot>
    getOptionChainSnapshot(filter: OptionChainFilter): Promise<OptionSnapshot[]>
    supportsStreaming(feed: MarketDataFeed): boolean
    connect(feeds?: MarketDataFeed[]): Promise<void>
    disconnect(): Promise<void>
    stream(feed: MarketDataFeed, symbols: string[]): Observable<StreamEvent<StockQuote | OptionSnapshot>>
  }
  ```

> Reconciliation note (2026-06-27): The option method is now singular `getOptionSnapshot(contractId)`, joined by `getOptionChainSnapshot(filter)`. `getActivities`, `getAccountInfo`, and `getMarketStatus` are **not** on this interface — they moved to a separate `BrokerProvider` (`broker:*` IPC). The concrete `supportsStreaming()` implementation in `MassiveMarketDataProvider` takes no argument and returns `true`.

- **Source:** `plans/us-31/data-model.md`, `plans/us-31/plan.md` Area 2
- **Implementation:** `src/main/integrations/market-data-provider.ts` (impl: `src/main/integrations/massive-market-data.ts`)

### `StockQuote` type

- **Type:** TypeScript type (returned from REST + stream paths)
- **Shape:**

  ```typescript
  {
    price: string // last trade or mid, 2dp
    bid: string // best bid, 2dp
    ask: string // best ask, 2dp
    change: string // daily change, 2dp
    changePercent: string // daily change %, 4dp
    prevClose: string // previous close, 2dp
    volume: number // daily volume
    timestamp: string // ISO-8601
  }
  ```

- **Validation:** Money fields are decimal strings (`price`/`bid`/`ask`/`change`/`prevClose` 2dp, `changePercent` 4dp). `volume` is a non-negative integer. `timestamp` is ISO-8601.
- **Source:** `plans/us-31/data-model.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): Under Massive, `change`/`changePercent`/`prevClose` come from the REST snapshot (`todaysChange`, `todaysChangePerc`, `prevDay.c`); stream (`AM`) ticks leave them as empty strings. `prevClose` was added and `changePercent` is now 4dp.

### `OptionSnapshot` type

- **Type:** TypeScript type
- **Shape:**

  ```typescript
  {
    bid: string // 2dp
    ask: string // 2dp
    mid: string // (bid + ask) / 2, 2dp — computed by adapter
    lastTrade: string // 2dp
    openInterest: number | null // null from the snapshot endpoint
    volume: number | null // null from the snapshot endpoint
    greeks?: {
      delta: string // 4dp
      gamma: string // 4dp
      theta: string // 4dp
      vega: string // 4dp
    }
    impliedVolatility?: string // 4dp
    timestamp: string // ISO-8601
  }
  ```

- **Validation:** `mid` is computed (not from API) as `(bid + ask) / 2` via decimal.js with `ROUND_HALF_UP`. Greeks and `impliedVolatility` are optional, 4dp decimal strings, populated only when Massive returns them.
- **Source:** `plans/us-31/data-model.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): `greeks` is now optional (`greeks?`) and `iv` became a separate optional top-level `impliedVolatility?` field rather than `greeks.iv`. `openInterest`/`volume` are `null` from Massive's snapshot endpoint.

### `OptionChainFilter` type

- **Type:** TypeScript type (added by the Massive migration)
- **Shape:**

  ```typescript
  type OptionChainFilter = {
    underlying: string
    expirationFrom?: string
    expirationTo?: string
    type?: 'put' | 'call'
    strikeFrom?: string
    strikeTo?: string
    limit?: number
    cursor?: string
  }
  ```

- **Behavior:** `getOptionChainSnapshot(filter)` returns `OptionSnapshot[]`, following Massive's `next_url` cursor pagination until exhausted (or stopping at `filter.limit`).
- **Source:** current source (`src/main/integrations/market-data-provider.ts`)
- **Implementation:** `src/main/integrations/market-data-provider.ts`

### `BrokerActivity` type and `ActivityFilter`

- **Type:** TypeScript types
- **Shape:**

  ```typescript
  interface BrokerActivity {
    activityId: string
    activityType: string // e.g., "OPASN", "OPEXP", "OPXRC"
    symbol: string
    qty: number
    price: string // 2dp
    transactionTime: string // ISO-8601
  }

  interface ActivityFilter {
    type: string // Activity type code (e.g., "OPASN")
    since?: string // ISO date string (YYYY-MM-DD)
  }
  ```

- **Validation:** Results sorted by `transactionTime` descending. `qty` is a positive integer.
- **Source:** `plans/us-31/data-model.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): Broker activities are no longer part of the `MarketDataProvider` interface; they moved onto the separate `BrokerProvider` (`broker:activities` IPC, `AlpacaBrokerProvider`).

### `AccountInfo` type

- **Type:** TypeScript type
- **Shape:**

  ```typescript
  {
    buyingPower: string
    portfolioValue: string
    cash: string
    environment: 'paper' | 'live'
  }
  ```

- **Validation:** `environment` is derived from the provider's `paper` config flag, not the API response.
- **Source:** `plans/us-31/data-model.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): Account info is no longer part of the `MarketDataProvider` interface; it moved onto the separate `BrokerProvider` (`broker:account` IPC, `AlpacaBrokerProvider`).

### `MarketStatus` type

- **Type:** TypeScript type
- **Shape:**

  ```typescript
  {
    isOpen: boolean
    nextOpen: string // ISO-8601
    nextClose: string // ISO-8601
    session: 'regular' | 'pre' | 'post' | 'closed'
  }
  ```

- **Validation:** `session` is derived from clock timestamp vs. calendar times (pre: 4:00–9:30 AM ET, regular: 9:30 AM–4:00 PM ET when `is_open`, post: 4:00–8:00 PM ET, closed: otherwise).
- **Source:** `plans/us-31/data-model.md`, `plans/us-31/research.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): Market status is no longer part of the `MarketDataProvider` interface; it moved onto the separate `BrokerProvider`, served by the `broker:market-status` IPC channel (there is no `market-data:market-status` channel).

### `MarketDataFeed`, `StreamEvent<T>`, `StreamError`

- **Type:** TypeScript types
- **Shape:**

  ```typescript
  type MarketDataFeed = 'stockQuotes' | 'optionQuotes' | 'optionTrades'

  interface StreamEvent<T> {
    feed: MarketDataFeed
    symbol: string
    data: T // StockQuote or OptionSnapshot depending on feed
    timestamp: string // ISO-8601
  }

  interface StreamError {
    feed: MarketDataFeed
    code: string // e.g., "stream_disconnected"
    message: string
    reconnectable: boolean
  }
  ```

- **Source:** `plans/us-31/data-model.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): The feed type was renamed `DataFeed` → `MarketDataFeed`; the three feed values are unchanged. In practice only `stockQuotes` streams (aggregate-minute `AM` bars); there is no live option stream.

### `MarketDataError` class

- **Type:** Error class
- **Shape:**

  ```typescript
  class MarketDataError extends Error {
    code:
      | 'auth_failed'
      | 'network_error'
      | 'not_found'
      | 'rate_limited'
      | 'streaming_unsupported'
      | 'unknown'
  }
  ```

- **Source:** `plans/us-31/data-model.md`, `plans/us-31/research.md`
- **Implementation:** `src/main/integrations/market-data-provider.ts`

> Reconciliation note (2026-06-27): Codes are now mapped from Massive's HTTP status (`401/403 → auth_failed`, `404 → not_found`, `429 → rate_limited` after retry). The original `stream_disconnected` and `subscription_failed` codes were dropped and `not_found` added.

### `marketDataFactory`

- **Type:** Factory object (not a function)
- **Shape:**

  ```typescript
  type MassiveMarketDataConfig = { apiKey: string }

  const marketDataFactory = {
    configure(next: MarketDataFactoryConfig): void // resets the cache
    create(): MarketDataProvider // cached
    recreate(): void // resets the cache
    disconnect(): Promise<void>
  }
  ```

- **Behavior:** `create()` returns a cached `FakeMarketDataProvider` when `FAKE_MARKET_DATA === 'true'`, otherwise a `MassiveMarketDataProvider` when `MASSIVE_API_KEY` is set, otherwise throws (`"Market data provider not configured. Set MASSIVE_API_KEY or FAKE_MARKET_DATA=true."`). The provider is env-var switched, not a `provider:` config union. The result is cached; `configure()`/`recreate()` reset the cache and `disconnect()` tears down the cached provider. Services import `marketDataFactory` and the `MarketDataProvider` interface — never the concrete provider class.
- **Source:** `plans/us-31/data-model.md`, `plans/us-31/plan.md` Area 5
- **Implementation:** `src/main/integrations/market-data-factory.ts`

> Reconciliation note (2026-06-27): The original `createMarketDataProvider(config: MarketDataConfig)` function and the `provider: 'alpaca'` config union are gone. Provider selection is now env-var driven (`FAKE_MARKET_DATA` / `MASSIVE_API_KEY`) through the `marketDataFactory` object's `.configure()`/`.create()`/`.recreate()`/`.disconnect()` methods, and the concrete provider is `MassiveMarketDataProvider`.

### WebSocket subscribe protocol

- **Type:** Wire protocol (Massive WebSocket — Polygon-compatible, JSON throughout)
- **Shape:**

  ```
  // Single stock socket: wss://delayed.massive.com/stocks
  1. Client connects to WebSocket
  2. On open, client: {"action":"auth","params":"<apiKey>"}
  3. Server: status/auth_success   (status/auth_failed rejects connect())
  4. Client: {"action":"subscribe","params":"AM.*"}
  5. Server: status/success        (resolves connect())
  6. Server pushes aggregate-minute (AM) bar frames thereafter

  // Symbol filtering happens in the Observable, NOT via a WS unsubscribe message.
  ```

- **WebSocket URL:** `wss://delayed.massive.com/stocks` — JSON text frames only, for aggregate-minute (`AM`) stock bars. There is no option WebSocket.
- **Unsubscribe:** There is no per-symbol WebSocket unsubscribe message — each `stream()` Observable filters the shared `Subject` by symbol, and `disconnect()` closes the single socket.
- **Source:** `plans/us-31/research.md`, `plans/us-31/plan.md` Area 4

> Reconciliation note (2026-06-27): Superseded by the Massive migration. The two Alpaca sockets (`wss://stream.data.alpaca.markets/...`, JSON stock + MessagePack option) and the explicit `unsubscribe` action are gone, replaced by the single JSON `wss://delayed.massive.com/stocks` socket above. The auth handshake is now `{action:'auth', params:<apiKey>}` rather than key/secret.

## Schema Changes

None — this story introduces no database tables, columns, or migrations. All types are in-memory TypeScript interfaces used at the integration boundary. (Source: `plans/us-31/data-model.md`)

## Acceptance Criteria

(Original 16 ACs as written for the Alpaca implementation. Source: `plans/us-31/plan.md` Area 7, `plans/us-31/red-phase-results.md`.)

> Reconciliation note (2026-06-27): These 16 ACs describe the original Alpaca surface and are kept for history. The current Massive-based provider surface is the 11-AC set in the feature page `docs/spec/features/us-31-market-data-provider-adapter.md`. Key differences: option retrieval is the singular `getOptionSnapshot` plus `getOptionChainSnapshot`; account info / activities / market status are now `BrokerProvider` concerns; `supportsStreaming()` takes no arg and returns `true`; streaming is JSON aggregate-minute (`AM`) bars only (no MessagePack option stream); error codes are HTTP-status-mapped (`auth_failed`/`network_error`/`not_found`/`rate_limited`); and there is no `*.e2e.test.ts` file — coverage lives in `massive-market-data.test.ts`, `market-data-provider.test.ts`, and `market-data-factory.test.ts`.

- **AC-1:** Interface exposes stock quote retrieval — `getStockQuotes(["AAPL","MSFT","TSLA"])` returns a `Map` with 3 entries, each `price` as a 2dp string.
- **AC-2:** Interface exposes option snapshot retrieval — `getOptionSnapshots(["AAPL260516P00180000"])` returns a `Map` entry with `bid`, `ask`, `mid` (= `(bid+ask)/2`), `greeks.delta`, `greeks.iv`, `timestamp`.
- **AC-3:** Interface exposes broker activity polling — `getActivities({ type: "OPASN", since: "2026-04-20" })` returns an array sorted by `transactionTime` descending with fields `activityId`, `activityType`, `symbol`, `qty`, `price`, `transactionTime`.
- **AC-4:** Interface exposes account info retrieval — `getAccountInfo()` returns `buyingPower`, `portfolioValue`, `cash`, `environment` (paper).
- **AC-5:** Interface exposes market status check — `getMarketStatus()` returns `isOpen`, `nextOpen`, `nextClose`, `session` (one of `regular | pre | post | closed`).
- **AC-6:** Provider declares streaming capabilities per feed — `supportsStreaming("stockQuotes" | "optionQuotes" | "optionTrades")` returns `true`.
- **AC-7:** Provider connects and streams stock quotes — after `connect()`, `stream("stockQuotes", ["AAPL","MSFT"]).subscribe(observer)` emits `StreamEvent<StockQuote>` for each tick.
- **AC-8:** Provider streams option quotes via MessagePack — `stream("optionQuotes", ["AAPL260516P00180000"]).subscribe(observer)` decodes MessagePack frames and emits `StreamEvent`.
- **AC-9:** Unsubscribe stops receiving events for those symbols — calling `subscription.unsubscribe()` causes the WebSocket to receive `unsubscribe` and observer receives no further emissions; WebSocket stays open for other subscriptions.
- **AC-10:** Disconnect closes all streams — `disconnect()` closes both sockets and all active subscribers receive `complete`.
- **AC-11:** Alpaca implementation connects using configured credentials — provider built with `paper: true` authenticates against paper endpoints; `getAccountInfo()` returns `environment: "paper"`.
- **AC-12:** Provider returns structured error when credentials are invalid — 401 from SDK becomes `MarketDataError` with `code: "auth_failed"`, message includes `"authentication"`.
- **AC-13:** Provider returns structured error when API is unreachable — network error becomes `MarketDataError` with `code: "network_error"`, message includes endpoint context.
- **AC-14:** Provider emits error event when stream disconnects unexpectedly — unexpected WebSocket close pushes `StreamError` through the Observable error channel with `code: "stream_disconnected"`, includes feed name and `reconnectable: true`.
- **AC-15:** Provider handles unknown ticker gracefully — `getStockQuotes(["AAPL","ZZZZZ"])` returns a `Map` with AAPL only, no error thrown.
- **AC-16:** Subscribe rejects unsupported feed — calling `stream(feed, ...)` where `supportsStreaming(feed)` is `false` throws `MarketDataError` with `code: "streaming_unsupported"`.

## Decisions & Tradeoffs

- **One factory branch per provider, services never import the concrete class.** Downstream code imports `marketDataFactory` and the `MarketDataProvider` interface only; the factory hides which provider is in use, enabling additional providers without churn. (Source: `plans/us-31/data-model.md`)
  > Reconciliation note (2026-06-27): originally `createMarketDataProvider` with a `provider:` config union; now the env-var-switched `marketDataFactory` object (`FakeMarketDataProvider` vs `MassiveMarketDataProvider`).
- **REST methods are `Promise`-returning; only streaming uses Observables.** Mixed paradigms intentionally — REST is request/response, streaming is push, and they should be modeled accordingly. (Source: `plans/us-31/research.md`)
- **`StockQuote.change` and `changePercent` were hardcoded to `'0.00'` in both REST and streaming paths** because previous-close data wasn't available from Alpaca's `getStocksQuotesLatest` or from stream frames. (Source: `plans/us-31/refactor-phase-layer3-results.md`, `plans/us-31/green-phase-streaming-results.md`)
  > Reconciliation note (2026-06-27): No longer hardcoded. Massive's REST snapshot supplies `change`/`changePercent`/`prevClose` (from `todaysChange`/`todaysChangePerc`/`prevDay.c`); only stream (`AM`) ticks leave them as empty strings, and US-32 carries the REST-seeded `prevClose` forward.
- **No reconnection logic in the provider.** `StreamError.reconnectable: true` signals intent, but the caller is responsible for retry/reconnect. Downstream stories will compose `retry`/`retryWhen` operators on the Observable. (Source: `plans/us-31/refactor-phase-layer3-results.md`, `plans/us-31/green-phase-streaming-results.md`)
- **A single `Subject` bridges the socket to Observable subscribers.** Each `stream()` call returns a new Observable that filters the shared `Subject<StreamEvent<StockQuote>>` by symbol (an empty symbol list matches all). (Source: `plans/us-31/plan.md` Area 4, `plans/us-31/green-phase-streaming-results.md`)
  > Reconciliation note (2026-06-27): With the single Massive socket there is one `Subject` (not one per socket), and there is no per-symbol WebSocket `unsubscribe` message — teardown just drops the Observable's symbol filter; `disconnect()` closes the socket.
- **`disconnect()` nulls out internal socket and Subject references after cleanup** to prevent accidental use of closed/completed resources. (Source: `plans/us-31/refactor-phase-layer3-results.md`)
- **Shared `mapQuoteToStockQuote(bp, ap, timestamp)` helper** kept REST and streaming codepaths consistent for `StockQuote` shape construction. (Source: `plans/us-31/refactor-phase-layer3-results.md`)
  > Reconciliation note (2026-06-27): this Alpaca-era helper no longer exists; the Massive provider maps Polygon-compatible snapshot/`AM`-bar shapes directly.
- **Test utilities (`emitSocketEvent`, `simulateAuth`, `MockSocket`) extracted to `alpaca-stream-test-utils.ts`** to avoid drift between unit and e2e tests; `connectAndAuth` kept per-file because factory vs direct-constructor versions differ. (Source: `plans/us-31/refactor-phase-results.md`)
  > Reconciliation note (2026-06-27): `alpaca-stream-test-utils.ts` still exists as a leftover; current streaming tests live in `massive-market-data.test.ts`.
- **All money values converted via `new Decimal(value).toFixed(2)` (or `.toFixed(4)` for greeks/IV)** — never floats in the public type surface; `mid` uses `ROUND_HALF_UP`. (Source: `plans/us-31/plan.md` Area 3)
- **OCC contract symbols are built by the shared `buildOccSymbol` helper in `src/shared/option-symbol.ts`** (re-exported from `src/main/core/option-symbol.ts`); `MassiveMarketDataProvider` prefixes the bare OCC symbol with `O:` (Polygon convention) at the API boundary and derives the underlying ticker by parsing the leading letters. (Source: current source)
- **Backend-only story — no UI, no IPC handlers, no Zod schemas.** All types are integration-layer TypeScript only; downstream UI stories (US-32–34) wire IPC and Zod on top. (Source: `plans/us-31/quickstart.md`, `plans/us-31/plan.md`)
- **Test file naming:** unit + integration tests live alongside the implementation (`*.test.ts`); end-to-end factory-driven tests live in `*.e2e.test.ts` colocated in the same dir. (Source: `plans/us-31/plan.md` Area 7)

## Source Code References

Verified to exist in the current tree (Glob-checked 2026-06-27):

- `src/main/integrations/market-data-provider.ts` — interface, all shared types, `MarketDataError` class
- `src/main/integrations/market-data-provider.test.ts` — type contract + `MarketDataError` tests
- `src/main/integrations/market-data-factory.ts` — `marketDataFactory` object (env-var switched)
- `src/main/integrations/market-data-factory.test.ts` — factory tests
- `src/main/integrations/massive-market-data.ts` — `MassiveMarketDataProvider` implementation (current concrete provider)
- `src/main/integrations/massive-market-data.test.ts` — unit + integration tests
- `src/main/integrations/fake-market-data.ts` — `FakeMarketDataProvider` (env-driven test/dev provider)
- `src/main/integrations/fake-market-data.test.ts` — fake-provider tests
- `src/main/integrations/integration-errors.ts` — shared `isNetworkError` helper
- `src/main/integrations/alpaca-stream-test-utils.ts` — shared `MockSocket`, `emitSocketEvent`, `simulateAuth` helpers (leftover from the original Alpaca streaming tests)
- `src/main/integrations/alpaca.ts` — pre-existing file, `@deprecated`
- `src/shared/option-symbol.ts` — `buildOccSymbol` (re-exported from `src/main/core/option-symbol.ts`)

Cited by the plan but **not present** in the current tree (Glob-confirmed absent — superseded by the Massive migration):

- `src/main/integrations/alpaca-market-data.ts` — was the planned `AlpacaMarketDataProvider`; replaced by `massive-market-data.ts` (`MassiveMarketDataProvider`). The only `Alpaca*` class that survives is `AlpacaBrokerProvider` in `src/main/integrations/alpaca-broker.ts`, which implements the separate `BrokerProvider` interface.
- `src/main/integrations/alpaca-market-data.test.ts`, `alpaca-market-data.e2e.test.ts` — gone with the implementation; there is no `*.e2e.test.ts` file (coverage is in `massive-market-data.test.ts`).

New dependencies introduced by this story (`package.json`):

- `ws` and `@types/ws` — WebSocket client for Electron main process
- `rxjs` — Observable abstraction for streaming
- `@msgpack/msgpack` — was added to decode the Alpaca option stream; still present in `package.json` but unused after the Massive migration (Massive streams JSON only)

## Open Questions

- **Reconnection logic deferred to consumers / future story.** `StreamError.reconnectable: true` is a hint, not a behavior — `retry`/`retryWhen` will be added by downstream stories (US-38). (Source: `plans/us-31/refactor-phase-layer3-results.md`)
- **Stream `change` / `changePercent` / `prevClose` ship as empty strings.** `AM` aggregate-minute bars don't carry daily change or previous close; the REST snapshot path populates them and US-32 carries the REST-seeded `prevClose` forward and computes the daily change renderer-side. (Source: `plans/us-31/refactor-phase-layer3-results.md`)
  > Reconciliation note (2026-06-27): the original "ships as `'0.00'`" limitation no longer applies to the REST path under Massive — only stream ticks leave these empty.
- **The concrete provider file may grow large.** The original `alpaca-market-data.ts` was ~400 lines at refactor end and flagged as splittable; that file is gone and the concern now applies to `massive-market-data.ts`. (Source: `plans/us-31/refactor-phase-layer3-results.md`)
- **Optional integration test against real provider credentials** is described in `quickstart.md` but not committed; the original used `describe.skipIf(!process.env.ALPACA_KEY_ID)`. (Source: `plans/us-31/quickstart.md`)
  > Reconciliation note (2026-06-27): the live-credential gate would now key off `MASSIVE_API_KEY` (or `FAKE_MARKET_DATA`).

<!-- /generated -->
