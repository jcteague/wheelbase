---
plan: us-37
source: plans/us-37/
extracted_at: 2026-06-06
status: complete
---

# Extract: us-37

<!-- generated -->

## Summary

Adds a settings workflow for broker credentials and runtime broker-environment switching without tying broker state to shared market-data configuration. Massive credentials remain shared app-level configuration and are never stored in user settings. Alpaca paper and live credentials are stored in SQLite via a new generic `credential_settings` table with `safeStorage.encryptString` encryption, while non-secret state such as the active broker environment is persisted in a new `app_settings` table. The renderer gets a dedicated `#/settings` route, per-environment Alpaca credential cards, a shared Massive status/test section, an always-visible `EnvironmentBadge`, a separate `MarketDataStatusDot`, and broker-only query invalidation keyed off `queryKey[0] === 'broker'`. Runtime broker changes recreate only the broker provider; market data continues uninterrupted. End-to-end coverage landed in `e2e/settings-environment.spec.ts`, covering vendor separation, test-connection flows, LIVE confirmation, secure persistence, stale-market-data fallback, and remembered broker environment. (Sources: `plans/us-37/plan.md`, `plans/us-37/quickstart.md`, `plans/us-37/refactor-phase-results.md`, `e2e/settings-environment.spec.ts`)

## Architecture Decisions

### ADR: Massive stays shared app configuration; user settings store Alpaca only

- **Decision:** Keep Massive credentials out of user settings entirely. The settings page can display Massive status and run a fixed reference probe, but there are no save/remove Massive settings channels and no Massive rows in `credential_settings`.
- **Why:** Product clarification changed the original story shape: Massive credentials are shared for all users and come from app configuration/env/packaging, while Alpaca paper/live credentials are user-specific.
- **Alternatives considered:** Per-user Massive credential storage in settings. Rejected because it conflicts with the clarified deployment model and would blur broker state with market-data state.
- **Source:** `plans/us-37/research.md`, `plans/us-37/data-model.md`, `plans/us-37/contracts/settings-ipc.md`

### ADR: Broker provider refresh is runtime-scoped; market-data provider is not recreated from settings

- **Decision:** `src/main/index.ts` registers broker handlers against a current-provider accessor so broker settings changes recreate only the broker provider cache. Market data keeps loading its shared Massive key from app configuration and is not recreated by settings page actions.
- **Why:** The story requires immediate broker-environment switching without interrupting ongoing quote/option data flows.
- **Alternatives considered:** Restart the app after credential changes; invalidate and recreate both provider stacks together. Both were rejected because they violate the scoped-refresh acceptance criteria.
- **Source:** `plans/us-37/research.md`, `plans/us-37/plan.md`, `plans/us-37/refactor-phase-results.md`

### ADR: Query invalidation is vendor-scoped by first key segment

- **Decision:** Normalize renderer query keys so broker reads start with `['broker', ...]` and market-data reads start with `['market', ...]`. Settings mutations invalidate broker queries via a predicate on `queryKey[0] === 'broker'` and separately refresh `settingsQueryKeys.status`.
- **Why:** The broker environment switch should refresh buying power, activities, and market-status surfaces without causing stock quote or option snapshot churn.
- **Alternatives considered:** Blanket invalidation after every settings mutation. Rejected because the story explicitly preserves uninterrupted market-data polling/streaming.
- **Source:** `plans/us-37/research.md`, `plans/us-37/plan.md`

### ADR: LIVE activation is a confirmed renderer action; switching back to paper is immediate

- **Decision:** The renderer shows a dedicated `LiveBrokerConfirmDialog` before invoking `settings:set-active-broker-environment` with `environment: 'live'`. The dialog includes copy about live-account reads, unsynced Wheelbase positions, unaffected Massive market data, and an open-position warning derived from existing position-list data. Switching from live back to paper has no dialog.
- **Why:** The story wants a visible risk boundary around live broker reads while keeping the paper path lightweight.
- **Source:** `plans/us-37/research.md`, `plans/us-37/quickstart.md`, `e2e/settings-environment.spec.ts`

## Contracts

### New persisted shapes

- **`credential_settings`** — generic credential table keyed by `(vendor, environment)` with encrypted `key_id_encrypted`, encrypted `secret_encrypted`, optional `last_verified_at`, masked account number, and timestamps. US-37 writes Alpaca rows only.
- **`app_settings`** — key/value settings table used here for `active_broker_environment = 'paper' | 'live' | 'none'`.
- **Source:** `plans/us-37/data-model.md`

### Settings IPC channels

- **`settings:get-credential-status`** — returns `CredentialStatus` with shared Massive status, per-environment Alpaca status, active broker environment, and masked account numbers.
- **`settings:save-alpaca-credentials`** — validates trimmed non-empty credentials, tests them, encrypts them, upserts the requested Alpaca environment, returns `{ status, test }`, and refreshes the broker provider only when the active environment changed or was updated.
- **`settings:remove-alpaca-credentials`** — deletes paper or live credentials, resets active broker environment to `none` when removing the active row, and refreshes broker state only when needed.
- **`settings:set-active-broker-environment`** — requires that the target environment has saved credentials, persists the active environment, and recreates only the broker provider.
- **`settings:test-connection`** — discriminated union payload: Massive runs a fixed `GET /v3/reference/tickers/AAPL` probe with the shared configured key; Alpaca runs `GET /v2/account` against paper or live using candidate credentials and returns the masked account number.
- **`settings:test-stored-alpaca-connection`** — uses already-saved encrypted credentials for a paper/live re-verification probe without exposing secrets back to the renderer.
- **Source:** `plans/us-37/contracts/settings-ipc.md`, `src/main/ipc/settings.ts`

### Renderer-facing state

- **`CredentialStatus`** — `{ massive, alpacaPaper, alpacaLive, activeBrokerEnv, massiveLastCheckedAt, alpacaPaperAccountNumberMasked, alpacaLiveAccountNumberMasked }`
- **`TestSettingsConnectionResult`** — success for Massive (`connected`) or Alpaca (`environment`, `accountNumberMasked`), or typed failure with `errorCode` and `message`
- **Source:** `plans/us-37/data-model.md`, `src/renderer/src/api/settings.ts`

## What was built

### Main-process persistence and probes

- Migration `006_add_credential_settings.sql` adds `credential_settings` and `app_settings`.
- `src/main/services/settings.ts` owns encrypted credential persistence, active-environment resolution, and masked status reads.
- `src/main/services/settings-connections.ts` implements Massive and Alpaca probe helpers with typed error mapping.
- `src/main/ipc/settings.ts` registers the settings channels, keeps handlers thin, and triggers broker refresh only when broker state changes.

### Provider lifecycle split

- `src/main/integrations/market-data-factory.ts` continues to load Massive from shared app configuration.
- `src/main/integrations/broker-factory.ts` now reads the active Alpaca environment from settings and recreates broker state at runtime.
- `src/main/index.ts` wires broker handlers against the current provider rather than a startup-only instance.

### Renderer data plumbing

- New `src/renderer/src/api/settings.ts` adapter and `useSettings.ts` hooks expose status, save/remove, environment switching, and connection tests.
- `brokerQueryKeys` and `marketDataQueryKeys` now separate broker and market namespaces cleanly (`['broker', ...]` vs `['market', ...]`).
- `useMarketStatus()` moves onto broker-prefixed keys so broker environment changes refresh account/clock surfaces without invalidating stock or option quote caches.

### Renderer UI and degraded states

- `#/settings` route and `SettingsPage` ship the revised vendor-separated layout: shared Massive section, paper/live Alpaca cards, segmented environment toggle, onboarding banner, and LIVE confirmation dialog.
- `EnvironmentBadge` is always visible and reflects broker state only: loud amber `PAPER`, subtle green `LIVE`, or neutral `NO BROKER`.
- `MarketDataStatusDot` reflects Massive status only and does not control the broker badge.
- Positions-list market-data failures show a stale-data banner and preserve stale values briefly before falling back to unavailable state, while broker-only surfaces prompt `Connect Alpaca to enable`.

### Verification status

- Unit/integration/refactor notes recorded in `plans/us-37/refactor-phase-results.md` show `pnpm test`, `pnpm lint`, and `pnpm typecheck` passing for the implementation phases.
- E2E coverage landed in `e2e/settings-environment.spec.ts`, including the scenarios that were still unchecked in `plans/us-37/tasks.md`.

## Decisions & tradeoffs

- **Secrets never round-trip to the renderer.** Saved cards render masked placeholders plus a `Replace` flow; the renderer never receives decrypted key material.
- **`CredentialStatus.massiveLastCheckedAt` remains `null`.** The status shape reserves it, but the current implementation does not persist a Massive probe timestamp.
- **Stored Alpaca re-test is implemented even though the original plan focused on candidate-key tests.** The additional `settings:test-stored-alpaca-connection` channel supports re-verification/re-entry flows without exposing saved credentials.
- **Market-data unavailability is vendor-specific.** Massive auth failures affect live quote surfaces and stale-data messaging; broker auth failures affect account/activity surfaces and the broker setup prompt.

## Source files

- `migrations/006_add_credential_settings.sql`
- `src/main/services/settings.ts`
- `src/main/services/settings-connections.ts`
- `src/main/ipc/settings.ts`
- `src/main/integrations/broker-factory.ts`
- `src/main/integrations/market-data-factory.ts`
- `src/main/index.ts`
- `src/main/schemas.ts`
- `src/preload/index.ts`
- `src/preload/index.d.ts`
- `src/renderer/src/api/settings.ts`
- `src/renderer/src/hooks/useSettings.ts`
- `src/renderer/src/hooks/settingsQueryKeys.ts`
- `src/renderer/src/hooks/brokerQueryKeys.ts`
- `src/renderer/src/hooks/marketDataQueryKeys.ts`
- `src/renderer/src/components/EnvironmentBadge.tsx`
- `src/renderer/src/components/MarketDataStatusDot.tsx`
- `src/renderer/src/components/LiveBrokerConfirmDialog.tsx`
- `src/renderer/src/components/StaleDataBanner.tsx`
- `src/renderer/src/pages/SettingsPage.tsx`
- `e2e/settings-environment.spec.ts`

## Open questions

None recorded in the plan artifacts. The remaining drift is clerical: `plans/us-37/tasks.md` still shows Layer 5 unchecked even though the e2e spec file is present and populated.

<!-- /generated -->
