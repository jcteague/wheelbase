---
title: "US-9: Record CC Expiring Worthless"
story: "As a wheel trader with an open covered call at expiration, I want to record that my covered call expired worthless, so that my position reflects that I still hold the shares and have collected the full CC premium, ready to sell the next covered call."
states: ["confirmation", "success"]
---

import { useState } from 'react'

# US-9: Record CC Expiring Worthless

_As a wheel trader with an open covered call at expiration, I want to record that my covered call expired worthless, so that my position reflects that I still hold the shares and have collected the full CC premium, ready to sell the next covered call._

## Acceptance Criteria States

- **confirmation**: Sheet open; position summary shows AAPL CALL $182.00 · Feb 21, 2026 · 1 contract · $2.30 premium; phase transition CC_OPEN → HOLDING_SHARES; premium-captured highlight row; irrevocable warning; Cancel + Confirm Expiration footer
- **success**: Green hero card "+$230.00 premium captured (100%)"; result summary card (leg recorded, phase back to HOLDING_SHARES, shares still held); strategic nudge "Many traders wait 1–3 days…"; "Sell New Covered Call on AAPL →" CTA

---

export const MOCK_POSITION = {
  ticker: 'AAPL',
  strike: '182.00',
  expiration: '2026-02-21',
  expirationDisplay: 'Feb 21, 2026',
  contracts: 1,
  premiumPerContract: '2.30',
  totalPremium: '230.00',
  sharesHeld: 100,
}

export const MONO = "'JetBrains Mono', 'Fira Mono', monospace"

// ── Primitive mocks — visually match the real components in src/renderer/src/components/ui/ ─────

// Matches: Badge.tsx
// Real usage: <Badge color="var(--wb-purple)">Call Open</Badge>
export function MockBadge({ children, color }) {
  const fg = color ?? 'var(--wb-gold, #e6a817)'
  return (
    <span style={{
      display: 'inline-flex',
      alignItems: 'center',
      fontSize: '0.65rem',
      fontWeight: 500,
      padding: '1px 7px',
      borderRadius: 10,
      background: fg + '18',
      color: fg,
      border: '1px solid ' + fg + '30',
      fontFamily: MONO,
    }}>
      {children}
    </span>
  )
}

// Matches: AlertBox.tsx — variant="warning" | "info"
// Real usage: <AlertBox variant="warning">This cannot be undone…</AlertBox>
//             <AlertBox variant="info">💡 Many traders wait…</AlertBox>
export function MockAlertBox({ variant, children }) {
  const styles = {
    warning: { bg: 'rgba(230,168,23,0.06)',  border: 'rgba(230,168,23,0.25)',  color: 'var(--wb-gold, #e6a817)' },
    info:    { bg: 'rgba(88,166,255,0.06)',   border: 'rgba(88,166,255,0.25)',  color: 'var(--wb-sky, #58a6ff)' },
    error:   { bg: 'rgba(248,81,73,0.06)',    border: 'rgba(248,81,73,0.25)',   color: 'var(--wb-red, #f85149)' },
    success: { bg: 'rgba(63,185,80,0.06)',    border: 'rgba(63,185,80,0.25)',   color: 'var(--wb-green, #3fb950)' },
  }
  const { bg, border, color } = styles[variant] ?? styles.warning
  return (
    <div style={{
      padding: '12px 16px',
      borderRadius: 6,
      background: bg,
      border: '1px solid ' + border,
      color,
      fontFamily: MONO,
      fontSize: '0.8125rem',
    }}>
      {children}
    </div>
  )
}

// Matches: SectionCard.tsx
// Real usage: <SectionCard header="Position Summary">…rows…</SectionCard>
export function MockSectionCard({ header, children }) {
  return (
    <section style={{
      background: 'var(--wb-bg-surface, #070a0e)',
      border: '1px solid var(--wb-border, #1e2a38)',
      borderRadius: 8,
      overflow: 'hidden',
    }}>
      {header && (
        <div style={{
          padding: '10px 20px',
          borderBottom: '1px solid var(--wb-border, #1e2a38)',
          fontSize: '0.65rem',
          fontWeight: 600,
          letterSpacing: '0.1em',
          textTransform: 'uppercase',
          color: 'var(--wb-text-muted, #4a5a6a)',
          fontFamily: MONO,
        }}>
          {header}
        </div>
      )}
      {children}
    </section>
  )
}

// Matches: FormButton.tsx (primary/gold submit button)
// Real usage: <FormButton label="Confirm Expiration" pendingLabel="Confirming…" isPending={isPending} />
export function MockFormButton({ label, isPending, onClick, style = {} }) {
  return (
    <button
      onClick={onClick}
      disabled={isPending}
      style={{
        padding: '11px 24px',
        borderRadius: 6,
        border: 'none',
        background: isPending ? 'rgba(230,168,23,0.4)' : 'var(--wb-gold, #e6a817)',
        color: '#000',
        fontSize: '0.9375rem',
        fontWeight: 600,
        fontFamily: MONO,
        cursor: isPending ? 'not-allowed' : 'pointer',
        letterSpacing: '0.04em',
        transition: 'opacity 0.15s',
        ...style,
      }}
    >
      {label}
    </button>
  )
}

// Matches: Button variant="outline" (shadcn ui/button)
// Real usage: <Button variant="outline" onClick={onClose}>Cancel</Button>
export function MockOutlineButton({ children, onClick, style = {} }) {
  return (
    <button
      onClick={onClick}
      style={{
        padding: '11px 24px',
        borderRadius: 6,
        border: '1px solid var(--wb-border, #2a3a4a)',
        background: 'transparent',
        color: 'var(--wb-text-secondary, #8899aa)',
        fontSize: '0.9375rem',
        fontWeight: 500,
        fontFamily: MONO,
        cursor: 'pointer',
        letterSpacing: '0.04em',
        ...style,
      }}
    >
      {children}
    </button>
  )
}

// Phase badge using Badge primitive colors
// Real usage: <PhaseBadge phase={phase} />
export function MockPhaseBadge({ phase }) {
  const map = {
    CSP_OPEN:       { label: 'Put Open',  color: '#e6a817' },
    HOLDING_SHARES: { label: 'Holding',   color: '#58a6ff' },
    CC_OPEN:        { label: 'Call Open', color: '#bc8cff' },
    CLOSED:         { label: 'Closed',    color: '#8899aa' },
  }
  const { label, color } = map[phase] ?? { label: phase, color: '#8899aa' }
  return <MockBadge color={color}>{label}</MockBadge>
}

// Summary row: used inside SectionCard for key/value pairs
export function MockSummaryRow({ label, value, noBorder, highlight }) {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'space-between',
      padding: '10px 14px',
      borderBottom: noBorder ? 'none' : '1px solid rgba(30,42,56,0.6)',
      background: highlight ? 'linear-gradient(90deg, rgba(63,185,80,0.06), transparent)' : undefined,
    }}>
      <span style={{ fontSize: 11, color: 'var(--wb-text-secondary, #8899aa)' }}>{label}</span>
      <span style={{ fontSize: 11, fontWeight: 600, color: 'var(--wb-text-primary, #e2eaf2)', textAlign: 'right' }}>
        {value}
      </span>
    </div>
  )
}

// ── Shared panel chrome ───────────────────────────────────────────────────────

export const panelStyle = {
  position: 'absolute',
  top: 0,
  right: 0,
  bottom: 0,
  width: 400,
  background: 'var(--wb-bg-surface, #070a0e)',
  borderLeft: '1px solid var(--wb-border, #1e2a38)',
  display: 'flex',
  flexDirection: 'column',
  fontFamily: MONO,
  boxShadow: '-12px 0 48px rgba(0,0,0,0.55)',
  color: 'var(--wb-text-primary, #e2eaf2)',
}

// ── Main Mockup ───────────────────────────────────────────────────────────────

export function Mockup() {
  const [state, setState] = useState('confirmation')
  const pos = MOCK_POSITION

  const states = ['confirmation', 'success']
  const isSuccess = state === 'success'

  return (
    <div style={{ fontFamily: MONO, background: '#040608', minHeight: 520 }}>

      {/* Dev state toggle */}
      <div style={{
        display: 'flex',
        gap: 6,
        padding: '10px 14px',
        background: '#0d1117',
        borderBottom: '1px solid #1e2a38',
        flexWrap: 'wrap',
      }}>
        <span style={{ fontSize: 9, color: '#4a5a6a', letterSpacing: '0.12em', textTransform: 'uppercase', alignSelf: 'center', marginRight: 4 }}>State:</span>
        {states.map(s => (
          <button
            key={s}
            onClick={() => setState(s)}
            style={{
              fontSize: 10,
              fontFamily: MONO,
              padding: '3px 10px',
              borderRadius: 4,
              border: '1px solid',
              borderColor: s === state ? 'var(--wb-gold, #e6a817)' : '#1e2a38',
              background: s === state ? 'rgba(230,168,23,0.12)' : 'transparent',
              color: s === state ? 'var(--wb-gold, #e6a817)' : '#6a7a8a',
              cursor: 'pointer',
              letterSpacing: '0.04em',
            }}
          >
            {s}
          </button>
        ))}
      </div>

      {/* Simulated content area with sheet overlay */}
      <div style={{ position: 'relative', height: 620, overflow: 'hidden', background: '#040608' }}>

        {/* Background: simulated position detail */}
        <div style={{ padding: '24px 28px', opacity: 0.25, pointerEvents: 'none', userSelect: 'none' }}>
          <div style={{ display: 'flex', alignItems: 'center', gap: 14, marginBottom: 20 }}>
            <span style={{ fontSize: 22, fontWeight: 700, color: '#e2eaf2', letterSpacing: '0.04em' }}>AAPL</span>
            <MockPhaseBadge phase="CC_OPEN" />
            <span style={{ fontSize: 11, color: '#8899aa' }}>CALL $182.00 · Feb 21, 2026 · 1 contract</span>
            <span style={{ marginLeft: 'auto' }}>
              {/* Real: <FormButton label="Record Expiration →" style={{ fontSize: 11, padding: '6px 14px' }} /> */}
              <MockFormButton label="Record Expiration →" style={{ fontSize: 11, padding: '6px 14px' }} />
            </span>
          </div>
          {/* Real: <StatGrid minWidth={120} items={[…]} /> from Stat.tsx */}
          <div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 1, background: '#1e2a38', borderRadius: 6, overflow: 'hidden' }}>
            {[['Strike', '$182.00'], ['Expiration', 'Feb 21'], ['Premium', '+$2.30'], ['Shares Held', '100']].map(([l, v]) => (
              <div key={l} style={{ padding: '14px 18px', background: '#070a0e' }}>
                <div style={{ fontSize: 9, color: '#4a5a6a', letterSpacing: '0.1em', textTransform: 'uppercase', marginBottom: 5 }}>{l}</div>
                <div style={{ fontSize: 13, fontWeight: 600, color: '#e2eaf2' }}>{v}</div>
              </div>
            ))}
          </div>
        </div>

        {/* Scrim */}
        <div style={{ position: 'absolute', inset: 0, background: 'rgba(4,6,8,0.45)' }} />

        {/* Sheet panel */}
        <div style={panelStyle}>

          {/* ── Header ── */}
          <div style={{
            padding: '20px 24px 18px',
            borderBottom: '1px solid ' + (isSuccess ? 'rgba(63,185,80,0.2)' : 'var(--wb-border, #1e2a38)'),
            display: 'flex',
            alignItems: 'flex-start',
            justifyContent: 'space-between',
            flexShrink: 0,
          }}>
            <div>
              {/* Real: <Caption>{isSuccess ? 'Complete' : 'Record Expiration'}</Caption> from Caption.tsx */}
              <div style={{
                fontSize: '0.65rem',
                fontWeight: 600,
                letterSpacing: '0.1em',
                textTransform: 'uppercase',
                marginBottom: 6,
                color: isSuccess ? 'var(--wb-green, #3fb950)' : 'var(--wb-text-secondary, #8899aa)',
                fontFamily: MONO,
              }}>
                {isSuccess ? 'Complete' : 'Record Expiration'}
              </div>
              <div style={{ fontSize: 17, fontWeight: 700, color: 'var(--wb-text-primary, #e2eaf2)', marginBottom: 4 }}>
                {isSuccess ? 'AAPL CC Expired Worthless' : 'Expire Covered Call Worthless'}
              </div>
              <div style={{ fontSize: 11, color: 'var(--wb-text-secondary, #8899aa)', lineHeight: 1.6 }}>
                CALL ${pos.strike} · {pos.expirationDisplay}
              </div>
            </div>
            <button style={{
              background: 'var(--wb-bg-elevated, #0d1821)',
              border: '1px solid var(--wb-border, #1e2a38)',
              color: 'var(--wb-text-muted, #4a5a6a)',
              fontSize: 14,
              width: 28,
              height: 28,
              borderRadius: 6,
              cursor: 'pointer',
              display: 'flex',
              alignItems: 'center',
              justifyContent: 'center',
              flexShrink: 0,
              marginLeft: 12,
              marginTop: 2,
            }}>×</button>
          </div>

          {/* ── Body ── */}
          <div style={{ padding: '20px 24px', overflowY: 'auto', flex: 1 }}>

            {isSuccess ? (
              <>
                {/* Hero card — green theme, premium captured */}
                <div style={{
                  background: 'linear-gradient(135deg, rgba(63,185,80,0.1), rgba(7,10,14,0.5))',
                  border: '1px solid rgba(63,185,80,0.22)',
                  borderRadius: 10,
                  padding: '22px 20px',
                  textAlign: 'center',
                  marginBottom: 18,
                }}>
                  {/* Real: <Caption>Premium Captured</Caption> */}
                  <div style={{ fontSize: 9, color: 'var(--wb-green, #3fb950)', opacity: 0.75, letterSpacing: '0.18em', textTransform: 'uppercase', marginBottom: 8 }}>
                    Premium Captured
                  </div>
                  <div style={{ fontSize: 40, fontWeight: 700, color: 'var(--wb-green, #3fb950)', letterSpacing: '-0.03em', lineHeight: 1, marginBottom: 6 }}>
                    +${pos.totalPremium}
                  </div>
                  <div style={{ fontSize: 11, color: 'var(--wb-green, #3fb950)', opacity: 0.65, marginBottom: 16 }}>
                    100% premium captured · {pos.contracts} contract
                  </div>
                  {/* Still holding shares badge */}
                  <div style={{
                    display: 'inline-flex',
                    alignItems: 'center',
                    gap: 8,
                    background: 'rgba(88,166,255,0.08)',
                    border: '1px solid rgba(88,166,255,0.2)',
                    borderRadius: 8,
                    padding: '8px 16px',
                  }}>
                    {/* Real: <Caption>Still Holding</Caption> */}
                    <span style={{ fontSize: 9, color: 'var(--wb-sky, #58a6ff)', opacity: 0.7, letterSpacing: '0.12em', textTransform: 'uppercase', fontFamily: MONO }}>Still Holding</span>
                    <span style={{ fontSize: 13, fontWeight: 700, color: 'var(--wb-sky, #58a6ff)', fontFamily: MONO }}>{pos.sharesHeld} shares of {pos.ticker}</span>
                  </div>
                </div>

                {/* Result summary card — Real: <SectionCard> with summary rows */}
                <div style={{ marginBottom: 16 }}>
                  <MockSectionCard>
                    <MockSummaryRow
                      label="Leg recorded"
                      value={<span style={{ color: 'var(--wb-green, #3fb950)' }}>expire · {pos.expirationDisplay}</span>}
                    />
                    <MockSummaryRow
                      label="Phase"
                      value={<MockPhaseBadge phase="HOLDING_SHARES" />}
                    />
                    <MockSummaryRow
                      label="Shares still held"
                      value={<span style={{ color: 'var(--wb-sky, #58a6ff)' }}>{pos.sharesHeld}</span>}
                    />
                    <MockSummaryRow
                      label="CC premium collected"
                      value={<span style={{ color: 'var(--wb-green, #3fb950)' }}>+${pos.totalPremium}</span>}
                      noBorder
                      highlight
                    />
                  </MockSectionCard>
                </div>

                {/* Strategic nudge — Real: <AlertBox variant="info">💡 Many traders wait…</AlertBox> */}
                <div style={{ marginBottom: 16 }}>
                  <MockAlertBox variant="info">
                    <span style={{ display: 'flex', gap: 8, alignItems: 'flex-start', lineHeight: 1.6 }}>
                      <span style={{ fontSize: 13, flexShrink: 0 }}>💡</span>
                      <span>Many traders wait <strong>1–3 days</strong> before selling the next covered call — avoid chasing premium right at expiration.</span>
                    </span>
                  </MockAlertBox>
                </div>

                {/* What's next */}
                {/* Real: <Caption>What's next?</Caption> */}
                <div style={{ fontSize: 9, fontWeight: 700, letterSpacing: '0.15em', textTransform: 'uppercase', color: 'var(--wb-text-muted, #4a5a6a)', marginBottom: 12 }}>
                  What&apos;s next?
                </div>

                {/* Real: <FormButton label={`Sell New Covered Call on ${ticker} →`} style={{ width: '100%' }} /> */}
                <MockFormButton
                  label={'Sell New Covered Call on ' + pos.ticker + ' →'}
                  onClick={() => {}}
                  style={{ width: '100%', marginBottom: 10 }}
                />

                <button onClick={() => {}} style={{
                  width: '100%',
                  marginTop: 2,
                  fontSize: 11,
                  color: 'var(--wb-text-secondary, #8899aa)',
                  background: 'none',
                  border: 'none',
                  cursor: 'pointer',
                  textDecoration: 'underline',
                  fontFamily: MONO,
                }}>
                  View full position history
                </button>
              </>
            ) : (
              <>
                {/* Summary card — Real: <SectionCard> with summary rows + premium highlight */}
                <div style={{ marginBottom: 18 }}>
                  <MockSectionCard>
                    <MockSummaryRow
                      label="Position"
                      value={pos.ticker + ' CALL $' + pos.strike + ' · Feb 21'}
                    />
                    <MockSummaryRow
                      label="Contracts"
                      value={pos.contracts}
                    />
                    <MockSummaryRow
                      label="Expiration date"
                      value={pos.expirationDisplay}
                    />
                    <MockSummaryRow
                      label="Phase transition"
                      value={
                        <span style={{ display: 'flex', alignItems: 'center', gap: 5 }}>
                          <MockPhaseBadge phase="CC_OPEN" />
                          <span style={{ fontSize: 9, color: 'var(--wb-text-muted, #4a5a6a)' }}>→</span>
                          <MockPhaseBadge phase="HOLDING_SHARES" />
                        </span>
                      }
                    />
                    <MockSummaryRow
                      label="Leg recorded"
                      value="expire · no fill price"
                    />
                    {/* Premium highlight row — green tint */}
                    <div style={{
                      display: 'flex',
                      alignItems: 'center',
                      justifyContent: 'space-between',
                      padding: '10px 14px',
                      borderBottom: 'none',
                      background: 'linear-gradient(90deg, rgba(63,185,80,0.06), transparent)',
                    }}>
                      <span style={{ fontSize: 11, color: 'var(--wb-green, #3fb950)', opacity: 0.8 }}>Premium captured</span>
                      <span style={{ fontSize: 14, fontWeight: 700, color: 'var(--wb-green, #3fb950)', fontFamily: MONO }}>
                        +${pos.totalPremium}{' '}
                        <span style={{ fontSize: 10, opacity: 0.65 }}>(100%)</span>
                      </span>
                    </div>
                  </MockSectionCard>
                </div>

                {/* Irrevocable warning — Real: <AlertBox variant="warning"><strong>This cannot be undone.</strong>…</AlertBox> */}
                <MockAlertBox variant="warning">
                  <strong style={{ fontWeight: 700, display: 'block', marginBottom: 3 }}>This cannot be undone.</strong>
                  The position will transition back to Holding Shares. Full leg history is preserved.
                </MockAlertBox>
              </>
            )}
          </div>

          {/* ── Footer ── */}
          {!isSuccess && (
            <div style={{
              padding: '16px 24px',
              borderTop: '1px solid var(--wb-border, #1e2a38)',
              display: 'flex',
              gap: 10,
              flexShrink: 0,
              background: 'var(--wb-bg-surface, #070a0e)',
            }}>
              {/* Real: <Button variant="outline" onClick={onClose} style={{ flex: 1 }}>Cancel</Button> */}
              <MockOutlineButton style={{ flex: 1 }}>Cancel</MockOutlineButton>
              {/* Real: <FormButton label="Confirm Expiration" pendingLabel="Confirming…" isPending={isPending} style={{ flex: 1 }} /> */}
              <MockFormButton
                label="Confirm Expiration"
                onClick={() => setState('success')}
                style={{ flex: 1, fontWeight: 700 }}
              />
            </div>
          )}

        </div>
      </div>
    </div>
  )
}

<Mockup />
